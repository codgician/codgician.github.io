name: builder
version: 0.1.0.0
github: "codgician/codgician.github.io"
license: BSD-3-Clause
author: "codgician"
maintainer: "codgician@outlook.com"
copyright: "2025 codgician"
extra-source-files:
  - README.md
  - CHANGELOG.md
description: Static site generator for codgician.me
dependencies:
  - base >= 4.7 && < 5
  - hakyll >= 4.16
  - pandoc >= 3.0
  - pandoc-types >= 1.23
  - text
  - bytestring
  - filepath
  - directory
  - process
  - yaml
  - aeson
  - time
  - containers
  - cryptohash-sha256
  - base16-bytestring
  - temporary
ghc-options:
  - -Wall
  - -Wcompat
  - -Widentities
  - -Wincomplete-record-updates
  - -Wincomplete-uni-patterns
  - -Wmissing-export-lists
  - -Wmissing-home-modules
  - -Wpartial-fields
  - -Wredundant-constraints
library:
  source-dirs: src
  exposed-modules:
    - Compiler.Cache
    - Compiler.KaTeX
    - Compiler.Mermaid
    - Compiler.Pandoc
    - Config
    - Context
    - Feed
    - Paginate
    - Site
executables:
  site:
    main: Main.hs
    source-dirs: app
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - builder
tests:
  builder-test:
    main: Spec.hs
    source-dirs: test
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - builder
      - hspec

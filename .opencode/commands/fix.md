---
description: Fix a bug (simple fixes skip planning)
agent: coder
subtask: true
---

## Bug Fix Request

**Issue**: $ARGUMENTS

**Recent changes for context**:
!`git diff HEAD~3 --stat`

Fix this issue with inline quality checks. Run `nix build` before completing.
